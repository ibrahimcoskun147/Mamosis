import 'package:flutter/material.dart';

/// App color constants following the Medical/Clean design system
class AppColors {
  AppColors._();

  // Primary Colors
  static const Color primary = Colors.black;
  static const Color primaryLight = Color(0xFF333333);
  
  // Background Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color cardBackground = Colors.white;
  
  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textHint = Color(0xFF999999);
  
  // Status Colors
  static const Color positive = Colors.black; 
  static const Color negative = Colors.black; 
  static const Color warning = Colors.black;
  
  // UI Colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);
  static const Color shadow = Color(0x1A000000);
  
  // Button Colors
  static const Color buttonPrimary = Colors.black;
  static const Color buttonDisabled = Color(0xFFBDBDBD);
  static const Color buttonText = Colors.white;
  
  // Accent Colors (for paywall, profile icons)
  static const Color accent = Color(0xFF333333);
  static const Color accentLight = Color(0xFFE8E6FF);
}
